<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.itzx.chat.mapper.ChatMessageMapper">

    <resultMap id="ChatMessageResultMap" type="com.itzx.chat.entity.ChatMessage">
        <id property="id" column="id"/>
        <result property="orderNo" column="order_no"/>
        <result property="fromRole" column="from_role"/>
        <result property="fromUsername" column="from_username"/>
        <result property="content" column="content"/>
        <result property="createTime" column="create_time"/>
    </resultMap>

    <insert id="insert" parameterType="com.itzx.chat.entity.ChatMessage" useGeneratedKeys="true" keyProperty="id">
        insert into order_chat_message(order_no, from_role, from_username, content, create_time)
        values (#{orderNo}, #{fromRole}, #{fromUsername}, #{content}, now())
    </insert>

    <select id="findByOrderNo" resultMap="ChatMessageResultMap">
        select id,order_no,from_role,from_username,content,create_time
        from order_chat_message
        where order_no = #{orderNo}
        order by create_time asc
    </select>

</mapper>
